import { CapacitorConfig } from '@capacitor/cli'

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ionic-vue-firebaseui-auth',
  webDir: 'dist',
  bundledWebRuntime: false
}

export default config
